import React, { memo } from 'react';
import styles from './progressBar.modules.scss';

const ProgressBarComponent = () => <div className={styles.progressBar}></div>;

export default memo(ProgressBarComponent);
